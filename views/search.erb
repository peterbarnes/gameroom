<ul>
  <% @results["users"].each do |user| %>
    <li class="person">
      <%= user["first_name"] %> <%= user["last_name"] %>
      <img src="<%= user["gravatar_url"] %>" />
    </li>
  <% end %>
</ul>